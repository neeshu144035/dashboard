I confirmed the authenticated Supabase MCP connection is pointed at the requested project: `https://uscotjkqicqxvddclglj.supabase.co`.

I stopped before execution because I could not read the local file `supabase/schema.sql`. Every attempt to use the local shell failed with the same infrastructure error:

```text
windows sandbox: spawn setup refresh
```

Because of that, I do not have the SQL text, and I will not execute an unknown or guessed schema against the real database. No SQL was applied, and no files were edited.

If you paste the contents of `supabase/schema.sql` here, I can apply it to the real Supabase database through the MCP connection and then verify the expected tables/functions/triggers exist.